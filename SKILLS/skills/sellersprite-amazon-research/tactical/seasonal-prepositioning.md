# 季节前置爆破

找去年同期搜索量暴增的关键词，提前布局。

## 工具
`mcp__sellersprite__keyword_miner` — `{"request": {...}}`
`mcp__sellersprite__keyword_research_trends` — 扁平参数

## 筛选
- 去年同比环比增长 > 100%
- 当前搜索量处于低点

## 注意
`keyword_research_trends` 字段：`time`/`search`/`chainGrowth`/`yearlyGrowth`

## 输出
季节性关键词表 + 增长曲线 + 建议布局时间
