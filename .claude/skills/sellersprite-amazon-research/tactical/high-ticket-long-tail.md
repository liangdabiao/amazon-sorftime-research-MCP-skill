# 高客单长尾

找均价≥$80 且搜索量适中的关键词（高利润长尾市场）。

## 工具
`mcp__sellersprite__keyword_miner` — `{"request": {...}}`
`mcp__sellersprite__keyword_research` — `{"request": {...}}`

## 筛选
- 客户端过滤 `avgPrice >= 80`
- `minSearch`: 500

## 输出
高客单关键词表 + 搜索量/供需比/竞价
